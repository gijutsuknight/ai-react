# Base URL for the backend API (e.g. Spring Boot).
# Use when the React app runs on a different origin (e.g. React :3000, backend :8080).
# Leave empty or omit to use the same origin (e.g. when served by the same host).
REACT_APP_API_BASE_URL=http://localhost:8080

# Spring Boot context path (must match server.servlet.context-path in application.properties).
REACT_APP_API_CONTEXT_PATH=/ai-springboot
